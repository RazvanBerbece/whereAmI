# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'whereAmI?' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'FontAwesome.swift'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Alamofire', '~> 5.1'

  # Pods for whereAmI?

  target 'whereAmI?Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'whereAmI?UITests' do
    # Pods for testing
  end

end
